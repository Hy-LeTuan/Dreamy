import Readable from '../type/stream.ts'
import Tree from '../type/tree.ts'

class Input extends Readable {
  constructor () {
    super()
  }
}

class Stream extends Input {

}

class Record {
  constructor () {
    this.input = new Input()

    this.input.on('data', () => this.data())
  }

  input
  immediate

  data () {
    if (!this.immediate) this.immediate = setImmediate(() => this.exec())
  }
  exec () {
    
  }
}
